{
  "name": "saint-firewall-controller",
  "version": "1.0.0",
  "private": true,
  "description": "Browser smoke tests for the SAINT admin UI.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
